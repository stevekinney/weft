# Workspace-level install configuration. The isolated linker gives every
# package its own node_modules view (matching the CI installs, which pass
# --linker=isolated), so per-package tooling resolves types and binaries
# from the package directory rather than relying on hoisting.
[install]
linker = "isolated"
