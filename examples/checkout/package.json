{
  "name": "weft-checkout-example",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint --type-aware --tsconfig ./tsconfig.json src tests",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "tests": "bun test",
    "typecheck": "tsc --noEmit",
    "verify": "bun run typecheck && bun run lint && bun run test"
  },
  "dependencies": {
    "@lostgradient/weft": "file:../.."
  },
  "devDependencies": {
    "@types/bun": "1.3.13",
    "oxlint": "1.56.0",
    "typescript": "5.9.3"
  }
}
